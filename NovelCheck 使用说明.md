# NovelCheck 使用说明

## 一、软件简介

**NovelCheck** 是一款 Windows 平台的小说文本智能校对工具，支持自动识别并修复小说文本中的多种问题：

| 功能 | 说明 |
|:---|:---|
| ? 错别字修正 | 识别形近字、音近字、常见错词和成语错字 |
| ? 广告检测与删除 | 自动移除嵌入正文中的网站广告（如"笔趣阁""奇书网"等） |
| ? 乱码清理 | 清除控制字符、零宽字符、编码错误产生的乱码 |
| ? 语义纠正 | 检测人物名称混淆等上下文相关错误 |
| ? AI 智能校对 | 接入大语言模型（本地 Ollama 或云端 API），对文本进行深度理解和修正 |

软件提供**传统模式**和 **AI 模式**两种纠错方式，可根据需要自由切换。

---

## 二、快速开始

### 2.1 直接运行 EXE（推荐）

双击 `NovelCheck.exe` 即可启动，无需安装任何环境。

> **首次启动须知：**
> - 首次运行可能需等待 5-10 秒进行初始化
> - 若 Windows Defender 弹出安全提示，请选择 **"更多信息" → "仍要运行"**
> - 建议将程序放置在**非中文路径**下（如 `D:\Tools\NovelCheck\`）

### 2.2 基本操作流程

```
导入文件 → 选择模式 → 开始纠错 → 查看结果 → 导出文件
```

---

## 三、使用教程

### 3.1 导入文件

1. 点击工具栏 **Import File**（或按 `Ctrl+O`）
2. 选择小说文件，支持 `.txt` 和 `.epub` 两种格式
3. 软件会自动检测文件编码（UTF-8、GBK、GB2312 等）
4. 导入成功后，原文显示在左侧面板

### 3.2 选择纠错模式

工具栏右侧有 **Mode** 按钮，可切换两种模式：

| 模式 | 说明 | 适用场景 |
|:---|:---|:---|
| **Traditional（传统）** | 基于规则的错别字、广告、乱码检测 | 快速处理，无需网络和额外配置 |
| **AI（智能）** | 调用大语言模型进行深度校对 | 更精确的纠错，能理解上下文语义 |

> 首次切换到 AI 模式时，需先配置 AI 设置（见第四章）。

### 3.3 开始纠错

1. 点击 **Start Correction**（或按 `Ctrl+R`）
2. 底部进度条会显示处理进度和预估剩余时间
3. AI 模式下可随时点击 **Cancel** 取消（已处理的部分会保留）

**传统模式处理流程：**
清理乱码 → 检测广告 → 修正错别字 → 检查语义错误

**AI 模式处理流程：**
清理乱码 → AI 大模型逐段校对（分批处理，约 3000 字/批）

### 3.4 查看结果

纠错完成后：

- **左右对比**：左侧原文 vs 右侧纠错后文本
- **高亮标记**：原文中的问题位置以不同颜色高亮
  - ? 黄色 — 乱码字符
  - ? 红色 — 广告内容
  - ? 蓝色 — 错别字
  - ? 绿色 — 语义错误
  - ? 紫色 — AI 修正
- **底部报告**：统计卡片展示各类问题数量，条形图显示问题分布

### 3.5 导出文件

1. 点击 **Export Result**（或按 `Ctrl+S`）
2. 选择保存位置和文件名
3. TXT 文件导出为 UTF-8 编码；EPUB 文件保留原格式结构

> 导出的是全新文件，**原始文件不会被修改**。

### 3.6 快捷键一览

| 快捷键 | 功能 |
|:---|:---|
| `Ctrl+O` | 导入文件 |
| `Ctrl+R` | 开始纠错 |
| `Ctrl+S` | 导出结果 |
| `Ctrl+Q` | 退出程序 |

---

## 四、AI 校对配置

点击工具栏右侧 **AI Settings** 按钮打开配置面板。

### 4.1 本地模式（Ollama）

使用本地大模型，无需联网，数据完全私密。

**配置步骤：**

1. 安装 Ollama：前往 [ollama.com](https://ollama.com) 下载安装
2. 拉取模型（在命令行执行）：
   ```
   ollama pull qwen2.5:7b-instruct
   ```
3. 在 AI Settings 中配置：
   - **Mode**: Local (Ollama)
   - **API Base URL**: `http://localhost:11434/v1`
   - **API Key**: 留空或填 `ollama`
   - **Model**: 点击 Fetch Models 自动获取，或手动输入 `qwen2.5:7b-instruct`
4. 点击 **Test Connection** 验证连接

**推荐模型：**

| 模型 | 显存需求 | 速度 | 质量 |
|:---|:---|:---|:---|
| `qwen2.5:3b` | ~3 GB | ? 快 | ★★★ |
| `qwen2.5:7b-instruct` | ~5.5 GB | ? 中 | ★★★★ |
| `qwen3:8b` | ~6 GB | ? 中 | ★★★★★ |
| `qwen2.5:14b` | ~10 GB | ? 慢 | ★★★★★ |

> **提示**：显存不足时模型会自动使用 CPU 内存运行，速度会明显下降。建议 7B 模型至少 8GB 显存。

### 4.2 云端 API 模式

使用第三方 API 服务，适合没有独立显卡的用户。

**配置步骤：**

1. 在 AI Settings 中配置：
   - **Mode**: Cloud API
   - **API Base URL**: 填写 API 服务商提供的地址
   - **API Key**: 填写你的 API Key
   - **Model**: 填写模型名称
2. 点击 **Test Connection** 验证

**支持的 API 平台示例：**

| 平台 | Base URL | 模型示例 |
|:---|:---|:---|
| 豆包（火山引擎） | `https://ark.cn-beijing.volces.com/api/v3` | `ep-xxxx`（端点 ID） |
| DeepSeek | `https://api.deepseek.com/v1` | `deepseek-chat` |
| API2D | `https://oa.api2d.net/v1` | `gpt-4o-mini` |
| OpenAI 官方 | `https://api.openai.com/v1` | `gpt-4o-mini` |

> **豆包用户注意**：模型名需填写端点 ID（以 `ep-` 开头），而非模型名称。

### 4.3 AI 配置文件

AI 设置保存在 `ai_config.json` 文件中（与 EXE 同目录）。首次运行后会自动创建，也可手动编辑：

```json
{
  "mode": "local",
  "model": "qwen2.5:7b-instruct",
  "base_url": "http://localhost:11434/v1",
  "api_key": ""
}
```

---

## 五、常见问题

### Q: 支持多大的文件？
**A:** 理论无上限。建议单个文件不超过 20MB。较大文件处理时间会相应增加，AI 模式下会自动分批处理。

### Q: AI 模式和传统模式有什么区别？
**A:** 传统模式基于规则匹配，速度快但只能检测已知模式的错误；AI 模式借助大语言模型理解上下文语义，能发现更多隐含错误，但需要更多时间和计算资源。

### Q: 纠错结果是否完全准确？
**A:** 两种模式都可能存在误判或漏判。建议自动纠错后人工复核重要段落，特别是语义层面的修改。

### Q: AI 模式处理很慢怎么办？
**A:** 可以尝试：
- 使用更小的模型（如 `qwen2.5:3b`）
- 使用云端 API 而非本地模型
- 检查 Ollama 是否正确使用了 GPU（`nvidia-smi` 查看 GPU 占用）

### Q: 首次启动 EXE 很慢？
**A:** 正常现象。`--onefile` 模式打包的程序首次运行需要解压资源到临时目录，后续启动会快很多。

### Q: EPUB 导出是否保留排版？
**A:** 是的。EPUB 导出基于原始文件结构，保留章节划分和基本格式。

### Q: 原始文件会被修改吗？
**A:** 不会。导出功能生成的是全新文件，原始文件始终不受影响。

---

## 六、系统要求

| 项目 | 最低要求 |
|:---|:---|
| 操作系统 | Windows 10 / 11（64 位） |
| 内存 | 4 GB RAM（AI 本地模式建议 16 GB+） |
| 磁盘空间 | 500 MB 可用空间 |
| 显示器 | 1280×720 或更高 |
| GPU（AI 本地模式） | NVIDIA 显卡，6 GB+ 显存（推荐） |

---

## 七、开发者指南（从源码运行）

如果你需要从源码运行或修改程序：

### 7.1 环境准备

1. 安装 Python 3.10+：[python.org](https://www.python.org/downloads/)（安装时勾选 **Add Python to PATH**）
2. 安装依赖：
   ```
   pip install -r requirements.txt
   ```
   或双击 `setup.bat`

### 7.2 运行程序

```
python main.py
```
或双击 `run.bat`

### 7.3 打包为 EXE

```
python build.py
```
或双击 `build_exe.bat`

打包完成后 EXE 位于 `dist\NovelCheck.exe`

### 7.4 依赖库

| 库名 | 用途 |
|:---|:---|
| PySide6 | Qt 图形界面框架 |
| jieba | 中文分词 |
| pypinyin | 拼音转换 |
| ebooklib | EPUB 读写 |
| beautifulsoup4 | HTML/XML 解析 |
| chardet | 编码自动检测 |
| lxml | XML 高效解析 |
| openai | AI API 客户端（AI 模式需要） |
| pyinstaller | 打包工具（仅打包时需要） |
