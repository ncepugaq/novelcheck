# NovelCheck 用户操作手册

## 一、软件简介

**NovelCheck** 是一款基于 Windows 平台的小说文本智能纠错工具，能够自动识别并修复小说文本中的以下问题：

- **错别字识别与修正**：精确识别中文常见错别字并给出修改建议
- **广告文字检测与删除**：识别并移除嵌入在正文中的广告内容（如"奇书网"、"笔趣阁"等）
- **乱码清理**：检测并删除文本中的乱码字符和无意义符号
- **语义错误纠正**：识别人物名称混淆等上下文相关错误

## 二、快速入门

### 2.1 一键启动（推荐）

```powershell
双击 run.bat → 自动启动程序
```

**首次使用前，请先运行环境安装：**
```powershell
双击 setup.bat → 自动安装Python依赖 → 安装完成后双击 run.bat
```

### 2.2 操作流程

```
导入文件 → 开始纠错 → 查看结果 → 导出文件
```

### 2.3 第一步：导入文件

1. 点击工具栏上的 **? 导入文件** 按钮（或按 **Ctrl+O**）
2. 在弹出的文件选择对话框中，选择您的小说文件
3. 支持的文件格式：`.txt`（文本文件）、`.epub`（EPUB电子书）
4. 软件会自动检测文件编码（UTF-8、GBK、GB2312 等）
5. 导入成功后，原文内容将显示在左侧面板中

### 2.4 第二步：纠错处理

1. 导入文件后，**? 开始纠错** 按钮变为可用（或按 **Ctrl+R**）
2. 点击该按钮，软件将自动执行以下处理流程：
   - 第一步：清理文本中的乱码字符
   - 第二步：检测并删除广告内容
   - 第三步：识别并修正错别字
   - 第四步：检查语义错误（如人物名称混淆）
3. 处理进度会在底部的进度条中显示
4. 处理完成后，右侧面板将显示纠错后的文本

### 2.5 第三步：查看结果

纠错完成后，您可以通过以下方式查看处理结果：

1. **左右对比**：左侧显示原文，右侧显示纠错后的文本
2. **高亮标记**：原文中的问题位置会以不同颜色高亮显示
   - 黄色：乱码字符
   - 红色/粉色：广告内容
   - 蓝色：错别字
   - 绿色：语义错误
3. **问题列表**：底部面板显示所有检测到的问题详情
4. **点击导航**：点击问题列表中的任意条目，自动跳转到原文对应位置

### 2.6 第四步：导出文件

1. 纠错完成后，点击 **? 导出结果** 按钮（或按 **Ctrl+S**）
2. 选择保存位置和文件名
3. 如目标文件已存在，会弹出覆盖确认提示
4. 支持导出为 `.txt` 格式
5. 如原始文件为 EPUB，也可导出为 EPUB 格式（保留原格式结构）

## 三、详细功能说明

### 3.1 错别字纠正

软件内置了丰富的混淆字集和常见错词库，能够识别：

- **形近字混淆**：如"己"误写为"已"，"干"误写为"千"
- **音近字混淆**：如"在"误写为"再"，"的"误写为"得"
- **常见错词**：如"寒喧"应为"寒暄"，"震憾"应为"震撼"
- **成语错字**：如"迫不急待"应为"迫不及待"、"甘败下风"应为"甘拜下风"

> **注意**：混淆字集采用上下文置信度评估，不会盲目替换所有匹配字符。例如"他在学校学习"中的"在"不会被替换为"载"。

### 3.2 广告文字检测

软件能够检测多种类型的广告内容：

- **完整广告行**：如"奇书网"、"笔趣阁"、"永久免费"等
- **嵌入式广告**：如广告文字散落在正文中的字符（如"晚风卷**奇**着落叶掠过巷口，昏**书**黄路灯..."中的"奇"和"书"嵌入"奇书网"广告）
- **URL链接和联系方式**：如网址、QQ群号、微信号
- **推广文本**：如"求收藏"、"求推荐"、"求订阅"等

### 3.3 乱码清理

支持清理的乱码类型：

- **控制字符和零宽字符**
- **非中文编码字符**：日文假名、韩文、希腊文、西里尔文等
- **常见乱码序列**：如"adde"、"rnm"、"rnmd"等
- **不可打印ASCII字符**

### 3.4 语义错误纠正

- **人物名称混淆检测**：通过频率分析，识别出场次数异常的疑似误写的人物名称

## 四、界面说明

### 4.1 工具栏

| 按钮 | 功能 | 快捷键 |
|:---|:---|:---|
| ? 导入文件 | 选择并加载小说文件 | Ctrl+O |
| ? 开始纠错 | 启动自动纠错处理 | Ctrl+R |
| ? 导出结果 | 保存纠错后的文本 | Ctrl+S |

其他快捷键：

| 快捷键 | 功能 |
|:---|:---|
| Ctrl+Q | 退出程序 |

### 4.2 文本区域

- **左侧（原文）**：显示导入的原始文本内容，问题位置会高亮显示
- **右侧（纠错结果）**：显示经过纠错处理后的干净文本

### 4.3 问题报告面板

位于界面底部，以表格形式展示所有检测到的问题：

| 列名 | 说明 |
|:---|:---|
| 类型 | 问题分类（乱码/广告/错别字/语义） |
| 行号 | 问题所在的文本行号 |
| 位置 | 问题在行内的字符位置 |
| 原文 | 检测到的原始文本 |
| 修改建议 | 推荐的修正文本 |
| 说明 | 问题检测的详细原因 |

### 4.4 状态栏

显示当前处理状态、问题统计信息和处理进度。

## 五、运行测试

验证软件功能是否正常：

```powershell
双击 run_tests.bat
```

或命令行运行：
```powershell
python test.py
```

## 六、打包为可执行文件

将软件打包为独立的 `.exe` 文件，无需安装 Python 即可运行：

```powershell
双击 build_exe.bat
```

打包完成后，可执行文件位于 `dist\NovelCheck.exe`

## 七、常见问题

### Q: 支持多大的文件？
A: 理论无上限，建议单个文件不超过 20MB，较大文件（>5MB）的处理时间会相应增加。

### Q: 纠错结果是否完全准确？
A: 软件基于规则匹配和统计分析，可能存在误判或漏判。建议在自动纠错后人工复核，特别是语义层面的修改建议。

### Q: 如何保留原始文件？
A: 导出的纠错文件是一个新文件，原始文件不会被修改。导出时如目标文件已存在会提示确认覆盖。

### Q: EPUB导出是否保留排版？
A: 是的。EPUB导出会基于原始EPUB的结构，保留章节划分和基本格式。

### Q: 如何验证软件是否正常工作？
A: 运行 `test.py` 或双击 `run_tests.bat`，将执行 38+ 项自动化测试覆盖所有核心功能。
