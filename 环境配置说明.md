# NovelCheck 环境配置说明

## 一、系统要求

| 项目 | 最低要求 |
|:---|:---|
| 操作系统 | Windows 10 / Windows 11（64位） |
| 内存 | 4 GB RAM |
| 磁盘空间 | 500 MB 可用空间 |
| 显示器 | 1280×720 分辨率或更高 |

## 二、一键安装与启动

### 2.1 首次使用

1. 双击 `setup.bat` — 自动检查 Python 环境并安装所有依赖
2. 双击 `run.bat` — 启动程序

### 2.2 后续使用

直接双击 `run.bat` 即可启动。

> **提示**：所有 `.bat` 文件需要以 UTF-8 编码运行（已内置 `chcp 65001`）。

## 三、使用预编译版本

### 3.1 打包为 EXE

如果你的环境中已配置好 Python，可以直接打包：

```powershell
双击 build_exe.bat
```

或命令行：
```powershell
python build.py
```

### 3.2 运行 EXE

打包完成后，`dist\NovelCheck.exe` 即可双击运行，无需 Python 环境。

### 3.3 注意事项

- 首次运行可能需要等待 5-10 秒初始化
- 如果 Windows Defender 弹出安全提示，请选择"仍要运行"
- 建议将软件放置在非中文路径

## 四、从源码运行（开发者）

### 4.1 安装 Python

1. 下载 Python 3.10 或更高版本：[python.org](https://www.python.org/downloads/)
2. 安装时**务必勾选 "Add Python to PATH"** 选项
3. 安装完成后，打开命令提示符验证：
   ```powershell
   python --version
   ```

### 4.2 安装依赖库

**自动安装（推荐）：**
```powershell
双击 setup.bat
```

**手动安装：**
```powershell
cd 项目目录
pip install -r requirements.txt
```

### 4.3 依赖库列表

| 库名 | 版本要求 | 用途 |
|:---|:---|:---|
| PySide6 | ≥6.5.0 | Qt图形界面框架 |
| jieba | ≥0.42.1 | 中文分词工具 |
| pypinyin | ≥0.49.0 | 拼音转换工具 |
| ebooklib | ≥0.18 | EPUB文件读写 |
| beautifulsoup4 | ≥4.12.0 | HTML/XML解析 |
| chardet | ≥5.1.0 | 编码自动检测 |
| lxml | ≥4.9.0 | XML高效解析 |
| pyinstaller | ≥5.13.0 | 程序打包工具（仅打包时需要） |

### 4.4 运行程序

```powershell
python main.py
```
或双击 `run.bat`

## 五、运行测试

```powershell
python test.py
```
或双击 `run_tests.bat`

测试覆盖：
- 错别字纠正（10项）
- 广告检测（8项）
- 乱码清理（8项）
- 语义纠正（3项）
- 集成测试（3项）
- 编码测试（3项）
- 边界情况（5项）

## 六、PyInstaller 打包详情

**自动打包：**
```powershell
双击 build_exe.bat
```

**手动打包命令：**
```powershell
pyinstaller --name=NovelCheck --windowed --onefile --clean `
    --add-data "novelcheck;novelcheck" `
    --hidden-import jieba `
    --hidden-import pypinyin `
    --collect-all jieba `
    --collect-all pypinyin `
    main.py
```

### 常见打包问题

**问题1：打包后运行报错"No module named 'novelcheck'"**
- 确保使用 `--add-data` 参数正确包含了 `novelcheck` 目录

**问题2：打包后 jieba 分词失败**
- 使用 `--collect-all jieba` 确保收集 jieba 的全部数据文件

**问题3：打包文件过大（>100MB）**
- 原因：PySide6 包含完整的 Qt 框架
- 建议：使用 UPX 压缩

## 七、项目目录结构

```
novelcheck/
├── main.py                     # 程序入口（含依赖检查）
├── build.py                    # 打包脚本
├── test.py                     # 自动化测试套件
├── requirements.txt            # Python依赖
├── setup.bat                   # 一键环境安装
├── run.bat                     # 一键启动
├── build_exe.bat               # 一键打包
├── run_tests.bat               # 一键测试
├── test_sample.txt             # 测试用小说样本
├── 用户操作手册.md              # 用户手册
├── 环境配置说明.md              # 本文档
│
├── novelcheck/                 # 主包
│   ├── __init__.py
│   │
│   ├── core/                   # 核心引擎
│   │   ├── __init__.py
│   │   ├── engine.py           # 统一纠错引擎
│   │   ├── corrector.py        # 错别字纠正
│   │   ├── ad_detector.py      # 广告检测
│   │   ├── garbled.py          # 乱码清理
│   │   └── semantic.py         # 语义纠正
│   │
│   ├── io/                     # 文件IO
│   │   ├── __init__.py
│   │   ├── txt_handler.py      # TXT读写
│   │   └── epub_handler.py     # EPUB读写
│   │
│   └── ui/                     # 用户界面
│       ├── __init__.py
│       └── main_window.py      # 主窗口
```
